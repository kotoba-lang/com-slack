;; Generated by scripts/bb_to_nbb_scaffold.cljs (ADR-2607173000).
;; Classpath for nbb. Prefer `nbb -m …` / package.json scripts over bb.edn.
{:paths ["src" "test"]
 :deps {
        ;; Declared HERE and not only in deps.edn: nbb reads nbb.edn and does NOT
        ;; read deps.edn or bb.edn, and the .cljs in this repo are run as
        ;; bare `nbb <file>`. A coordinate they cannot see is not a
        ;; declaration. sha and :paths are COPIED from deps.edn.
        io.github.kotoba-lang/fmt {:git/url "https://github.com/kotoba-lang/fmt.git" :git/sha "9202d2c30fd4aa0a0c584b95644eaf5b6b19c293"}}}
